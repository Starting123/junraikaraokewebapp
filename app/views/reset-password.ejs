<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รีเซ็ตรหัสผ่าน - Junrai Karaoke</title>
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="stylesheet" href="/stylesheets/auth.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .reset-password-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #d2691e 0%, #8b4513 100%);
            padding: 20px;
        }
        
        .reset-password-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .logo-section {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo-icon {
            font-size: 48px;
            color: #d2691e;
            margin-bottom: 10px;
        }
        
        .logo-text {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .subtitle {
            color: #666;
            font-size: 14px;
        }
        
        .form-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .form-title {
            font-size: 28px;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }
        
        .form-description {
            color: #666;
            font-size: 16px;
            line-height: 1.5;
        }
        
        .user-info {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 12px;
            padding: 15px 20px;
            margin-bottom: 25px;
            text-align: center;
        }
        
        .user-info .user-email {
            font-weight: 600;
            color: #333;
        }
        
        .form-group {
            margin-bottom: 25px;
            position: relative;
        }
        
        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        
        .form-input {
            width: 100%;
            padding: 15px 50px 15px 20px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: #fff;
        }
        
        .form-input:focus {
            outline: none;
            border-color: #d2691e;
            box-shadow: 0 0 0 3px rgba(210, 105, 30, 0.1);
        }
        
        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: #6c757d;
            cursor: pointer;
            font-size: 18px;
            padding: 5px;
        }
        
        .password-toggle:hover {
            color: #d2691e;
        }
        
        .password-strength {
            margin-top: 10px;
            font-size: 14px;
        }
        
        .strength-meter {
            height: 4px;
            background: #e1e5e9;
            border-radius: 2px;
            margin: 5px 0;
            overflow: hidden;
        }
        
        .strength-bar {
            height: 100%;
            transition: all 0.3s ease;
            border-radius: 2px;
        }
        
        .strength-weak .strength-bar {
            width: 33%;
            background: #dc3545;
        }
        
        .strength-medium .strength-bar {
            width: 66%;
            background: #ffc107;
        }
        
        .strength-strong .strength-bar {
            width: 100%;
            background: #28a745;
        }
        
        .btn-primary {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #d2691e, #a0522d);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(210, 105, 30, 0.3);
        }
        
        .btn-primary:disabled {
            opacity: 0.7;
            cursor: not-allowed;
            transform: none;
        }
        
        .btn-loading {
            opacity: 0.8;
            pointer-events: none;
        }
        
        .loading-spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top: 2px solid white;
            animation: spin 1s linear infinite;
            margin-right: 10px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .alert {
            padding: 15px 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            font-weight: 500;
            display: none;
        }
        
        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        
        .back-to-login {
            text-align: center;
            margin-top: 25px;
        }
        
        .back-to-login a {
            color: #d2691e;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }
        
        .back-to-login a:hover {
            color: #a0522d;
            text-decoration: underline;
        }
        
        .password-requirements {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 12px;
            padding: 15px 20px;
            margin-top: 15px;
            font-size: 14px;
        }
        
        .password-requirements h4 {
            color: #495057;
            margin-bottom: 10px;
            font-size: 14px;
            font-weight: 600;
        }
        
        .requirements-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .requirements-list li {
            padding: 3px 0;
            color: #6c757d;
            position: relative;
            padding-left: 25px;
        }
        
        .requirements-list li::before {
            content: '✗';
            position: absolute;
            left: 0;
            color: #dc3545;
            font-weight: bold;
        }
        
        .requirements-list li.valid::before {
            content: '✓';
            color: #28a745;
        }
        
        .token-expired {
            text-align: center;
            padding: 40px 20px;
        }
        
        .expired-icon {
            font-size: 64px;
            color: #dc3545;
            margin-bottom: 20px;
        }
        
        .expired-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }
        
        .expired-message {
            color: #666;
            margin-bottom: 25px;
            line-height: 1.6;
        }
        
        @media (max-width: 600px) {
            .reset-password-card {
                padding: 30px 20px;
                margin: 10px;
            }
            
            .form-title {
                font-size: 24px;
            }
            
            .logo-icon {
                font-size: 40px;
            }
        }
    </style>
</head>
<body>
    <div class="reset-password-container">
        <div class="reset-password-card">
            <!-- Logo Section -->
            <div class="logo-section">
                <div class="logo-icon">
                    <i class="fas fa-microphone-alt"></i>
                </div>
                <div class="logo-text">Junrai Karaoke</div>
                <div class="subtitle">ระบบจัดการคาราโอเกะ</div>
            </div>
            
            <!-- Loading State -->
            <div id="loadingState" style="text-align: center; padding: 40px;">
                <i class="fas fa-spinner fa-spin fa-3x" style="color: #d2691e; margin-bottom: 20px;"></i>
                <p>กำลังตรวจสอบโทเคน...</p>
            </div>
            
            <!-- Token Expired State -->
            <div id="tokenExpired" class="token-expired" style="display: none;">
                <div class="expired-icon">
                    <i class="fas fa-clock"></i>
                </div>
                <h2 class="expired-title">โทเคนหมดอายุแล้ว</h2>
                <p class="expired-message">
                    ลิงก์รีเซ็ตรหัสผ่านนี้หมดอายุแล้วหรือถูกใช้ไปแล้ว<br>
                    กรุณาขอรีเซ็ตรหัสผ่านใหม่อีกครั้ง
                </p>
                <a href="/forgot-password" class="btn-primary" style="display: inline-block; text-decoration: none; max-width: 250px;">
                    ขอรีเซ็ตรหัสผ่านใหม่
                </a>
            </div>
            
            <!-- Reset Password Form -->
            <div id="resetPasswordContent" style="display: none;">
                <!-- Form Header -->
                <div class="form-header">
                    <h1 class="form-title">
                        <i class="fas fa-key"></i>
                        รีเซ็ตรหัสผ่าน
                    </h1>
                    <p class="form-description">
                        กรุณาใส่รหัสผ่านใหม่สำหรับบัญชีของคุณ
                    </p>
                </div>
                
                <!-- User Info -->
                <div class="user-info" id="userInfo" style="display: none;">
                    <i class="fas fa-user-circle" style="color: #d2691e; margin-right: 8px;"></i>
                    รีเซ็ตรหัสผ่านสำหรับ: <span class="user-email" id="userEmail"></span>
                </div>
                
                <!-- Alert Messages -->
                <div id="alertContainer"></div>
                
                <!-- Reset Password Form -->
                <form id="resetPasswordForm">
                    <input type="hidden" id="resetToken" value="<%= token %>">
                    
                    <div class="form-group">
                        <label for="password" class="form-label">
                            <i class="fas fa-lock"></i>
                            รหัสผ่านใหม่
                        </label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            class="form-input" 
                            placeholder="กรอกรหัสผ่านใหม่"
                            required
                            autocomplete="new-password"
                        >
                        <button type="button" class="password-toggle" onclick="togglePassword('password')">
                            <i class="fas fa-eye" id="passwordToggleIcon"></i>
                        </button>
                        
                        <!-- Password Strength Meter -->
                        <div class="password-strength" id="passwordStrength" style="display: none;">
                            <div class="strength-meter">
                                <div class="strength-bar" id="strengthBar"></div>
                            </div>
                            <div class="strength-text" id="strengthText"></div>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="confirmPassword" class="form-label">
                            <i class="fas fa-lock"></i>
                            ยืนยันรหัสผ่าน
                        </label>
                        <input 
                            type="password" 
                            id="confirmPassword" 
                            name="confirmPassword" 
                            class="form-input" 
                            placeholder="ยืนยันรหัสผ่านใหม่"
                            required
                            autocomplete="new-password"
                        >
                        <button type="button" class="password-toggle" onclick="togglePassword('confirmPassword')">
                            <i class="fas fa-eye" id="confirmPasswordToggleIcon"></i>
                        </button>
                    </div>
                    
                    <!-- Password Requirements -->
                    <div class="password-requirements">
                        <h4>
                            <i class="fas fa-shield-alt"></i>
                            ข้อกำหนดรหัสผ่าน
                        </h4>
                        <ul class="requirements-list">
                            <li id="req-length">อย่างน้อย 6 ตัวอักษร</li>
                            <li id="req-lowercase">ตัวพิมพ์เล็ก (a-z)</li>
                            <li id="req-uppercase">ตัวพิมพ์ใหญ่ (A-Z)</li>
                            <li id="req-number">ตัวเลข (0-9)</li>
                            <li id="req-match">รหัสผ่านตรงกัน</li>
                        </ul>
                    </div>
                    
                    <button type="submit" class="btn-primary" id="submitBtn">
                        <div class="loading-spinner"></div>
                        <span class="btn-text">รีเซ็ตรหัสผ่าน</span>
                    </button>
                </form>
                
                <!-- Back to Login -->
                <div class="back-to-login">
                    <a href="/auth">
                        <i class="fas fa-arrow-left"></i>
                        กลับไปเข้าสู่ระบบ
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const token = document.getElementById('resetToken').value;
            const loadingState = document.getElementById('loadingState');
            const tokenExpired = document.getElementById('tokenExpired');
            const resetPasswordContent = document.getElementById('resetPasswordContent');
            const form = document.getElementById('resetPasswordForm');
            const submitBtn = document.getElementById('submitBtn');
            const btnText = submitBtn.querySelector('.btn-text');
            const loadingSpinner = submitBtn.querySelector('.loading-spinner');
            const alertContainer = document.getElementById('alertContainer');
            
            // Validate token on page load
            validateToken();
            
            async function validateToken() {
                try {
                    const response = await fetch(`/api/auth/validate-reset-token/${token}`);
                    const data = await response.json();
                    
                    loadingState.style.display = 'none';
                    
                    if (data.valid) {
                        resetPasswordContent.style.display = 'block';
                        
                        // Show user info
                        const userInfo = document.getElementById('userInfo');
                        const userEmail = document.getElementById('userEmail');
                        userEmail.textContent = data.user.email;
                        userInfo.style.display = 'block';
                        
                        // Focus password input
                        document.getElementById('password').focus();
                        
                        // Set up form validation
                        setupFormValidation();
                        
                    } else {
                        tokenExpired.style.display = 'block';
                    }
                    
                } catch (error) {
                    console.error('Error validating token:', error);
                    loadingState.style.display = 'none';
                    tokenExpired.style.display = 'block';
                }
            }
            
            function setupFormValidation() {
                const passwordInput = document.getElementById('password');
                const confirmPasswordInput = document.getElementById('confirmPassword');
                
                // Password strength validation
                passwordInput.addEventListener('input', function() {
                    validatePasswordStrength(this.value);
                    validatePasswordMatch();
                });
                
                // Confirm password validation
                confirmPasswordInput.addEventListener('input', validatePasswordMatch);
                
                // Form submission
                form.addEventListener('submit', async function(e) {
                    e.preventDefault();
                    
                    if (!validateForm()) {
                        return;
                    }
                    
                    setLoadingState(true);
                    
                    try {
                        const response = await fetch(`/api/auth/reset-password/${token}`, {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify({
                                password: passwordInput.value,
                                confirmPassword: confirmPasswordInput.value
                            })
                        });
                        
                        const data = await response.json();
                        
                        if (data.success) {
                            showAlert(data.message, 'success');
                            
                            // Redirect to login after success
                            setTimeout(() => {
                                window.location.href = '/auth?message=รีเซ็ตรหัสผ่านสำเร็จ กรุณาเข้าสู่ระบบด้วยรหัสผ่านใหม่';
                            }, 3000);
                            
                        } else {
                            showAlert(data.error || 'เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง', 'error');
                        }
                        
                    } catch (error) {
                        console.error('Error:', error);
                        showAlert('เกิดข้อผิดพลาดในการเชื่อมต่อ กรุณาลองใหม่อีกครั้ง', 'error');
                    } finally {
                        setLoadingState(false);
                    }
                });
            }
            
            function validatePasswordStrength(password) {
                const strengthIndicator = document.getElementById('passwordStrength');
                const strengthBar = document.getElementById('strengthBar');
                const strengthText = document.getElementById('strengthText');
                
                if (!password) {
                    strengthIndicator.style.display = 'none';
                    return;
                }
                
                strengthIndicator.style.display = 'block';
                
                let score = 0;
                const requirements = {
                    length: password.length >= 6,
                    lowercase: /[a-z]/.test(password),
                    uppercase: /[A-Z]/.test(password),
                    number: /\d/.test(password)
                };
                
                // Update requirement indicators
                document.getElementById('req-length').classList.toggle('valid', requirements.length);
                document.getElementById('req-lowercase').classList.toggle('valid', requirements.lowercase);
                document.getElementById('req-uppercase').classList.toggle('valid', requirements.uppercase);
                document.getElementById('req-number').classList.toggle('valid', requirements.number);
                
                // Calculate strength
                Object.values(requirements).forEach(req => {
                    if (req) score++;
                });
                
                // Update strength meter
                strengthIndicator.className = 'password-strength';
                if (score < 2) {
                    strengthIndicator.classList.add('strength-weak');
                    strengthText.textContent = 'รหัสผ่านอ่อน';
                } else if (score < 4) {
                    strengthIndicator.classList.add('strength-medium');
                    strengthText.textContent = 'รหัสผ่านปานกลาง';
                } else {
                    strengthIndicator.classList.add('strength-strong');
                    strengthText.textContent = 'รหัสผ่านแข็งแกร่ง';
                }
                
                return score >= 4;
            }
            
            function validatePasswordMatch() {
                const password = document.getElementById('password').value;
                const confirmPassword = document.getElementById('confirmPassword').value;
                
                const matchReq = document.getElementById('req-match');
                const isMatch = password && confirmPassword && password === confirmPassword;
                
                matchReq.classList.toggle('valid', isMatch);
                
                return isMatch;
            }
            
            function validateForm() {
                const password = document.getElementById('password').value;
                const confirmPassword = document.getElementById('confirmPassword').value;
                
                if (!password) {
                    showAlert('กรุณากรอกรหัสผ่านใหม่', 'error');
                    return false;
                }
                
                if (!validatePasswordStrength(password)) {
                    showAlert('รหัสผ่านไม่ตรงตามข้อกำหนด', 'error');
                    return false;
                }
                
                if (!validatePasswordMatch()) {
                    showAlert('รหัสผ่านไม่ตรงกัน', 'error');
                    return false;
                }
                
                return true;
            }
            
            // Helper functions
            function showAlert(message, type) {
                alertContainer.innerHTML = `
                    <div class="alert alert-${type}">
                        <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-triangle'}"></i>
                        ${message}
                    </div>
                `;
                
                alertContainer.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            }
            
            function setLoadingState(loading) {
                if (loading) {
                    submitBtn.classList.add('btn-loading');
                    loadingSpinner.style.display = 'inline-block';
                    btnText.textContent = 'กำลังรีเซ็ต...';
                    submitBtn.disabled = true;
                } else {
                    submitBtn.classList.remove('btn-loading');
                    loadingSpinner.style.display = 'none';
                    btnText.textContent = 'รีเซ็ตรหัสผ่าน';
                    submitBtn.disabled = false;
                }
            }
        });
        
        // Password toggle functionality
        function togglePassword(inputId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(inputId + 'ToggleIcon');
            
            if (input.type === 'password') {
                input.type = 'text';
                icon.className = 'fas fa-eye-slash';
            } else {
                input.type = 'password';
                icon.className = 'fas fa-eye';
            }
        }
    </script>
</body>
</html>