<nav class="main-navbar" id="mainNavbar">
    <div class="container">
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'home' ? 'active' : '' %>">
                    <i class="fas fa-home"></i>
                    <span>Home</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="/rooms" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'rooms' ? 'active' : '' %>">
                    <i class="fas fa-music"></i>
                    <span>Rooms</span>
                </a>
            </li>
            <% if (typeof user !== 'undefined' && user) { %>
                <li class="nav-item">
                    <a href="/bookings" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'bookings' ? 'active' : '' %>">
                        <i class="fas fa-calendar-check"></i>
                        <span>My Bookings</span>
                    </a>
                </li>
            <% } %>
            <li class="nav-item">
                <a href="/contact" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'contact' ? 'active' : '' %>">
                    <i class="fas fa-envelope"></i>
                    <span>Contact</span>
                </a>
            </li>
            <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                <li class="nav-item admin-only">
                    <a href="/admin/dashboard" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'admin' ? 'active' : '' %>">
                        <i class="fas fa-cog"></i>
                        <span>Admin</span>
                    </a>
                </li>
            <% } %>
        </ul>
        
        <!-- Mobile menu overlay -->
        <div class="mobile-overlay" id="mobileOverlay"></div>
    </div>
</nav>